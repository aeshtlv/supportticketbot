# Telegram Support Bot

Telegram-бот для организации системы поддержки с пересылкой сообщений в групповой чат и поддержкой топиков форума.

## 🚀 Возможности

- 🔄 **Двусторонняя связь**: Пересылка сообщений от пользователей в чат поддержки и обратно через Reply
- 📁 **Гибкая система топиков**:
  - **Режим "Отдельный топик для каждого"** — автоматическое создание персонального топика для каждого пользователя с отображением статуса (🟢 открыт / 🔴 закрыт)
  - **Режим "Общий топик"** — все тикеты в одном топике (классический режим)
  - Переключение режимов через админ-панель без перезапуска бота
- ⚙️ **Админ-панель**: Администраторы могут менять текст приветствия (/start) и справки (/help) прямо через бота
- ⛔️ **Блокировка пользователей**: Удобные инлайн-кнопки для бана/разбана пользователей прямо в интерфейсе чата
- 🎫 **Система тикетов**: Автоматическое создание уникального ID тикета для каждого пользователя (формат: SHFT-XXXX)
- 📊 **Управление статусами**: open/closed, команды /close, /reopen
- 📈 **Список тикетов**: /open_tickets — все открытые тикеты с деталями
- 📤 **Поддержка всех медиа**: Текст, фото, видео, документы, голосовые, аудио, стикеры, GIF
- 💾 **База данных**: Использует SQLite для хранения связей сообщений, черного списка и тексты настроек бота

## 📋 Установка

### 1. Клонируйте репозиторий
```bash
cd supportticketbot
```

### 2. Создайте виртуальное окружение
```bash
python -m venv venv
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate
```

### 3. Установите зависимости
```bash
pip install -r requirements.txt
```

### 4. Настройте переменные окружения

Создайте файл `.env` в корне проекта:
```env
BOT_TOKEN=your_telegram_bot_token_here
SUPPORT_CHAT_ID=-1001234567890
ADMIN_IDS=123456789,987654321
```

- `BOT_TOKEN` — токен бота от [@BotFather](https://t.me/BotFather)
- `SUPPORT_CHAT_ID` — ID группы/супергруппы для поддержки (можно получить через [@userinfobot](https://t.me/userinfobot))
- `ADMIN_IDS` — Telegram ID администраторов через запятую

**Важно**: Для режима "Отдельный топик для каждого" чат поддержки должен быть супергруппой с включёнными топиками (Forum).

### 5. Запустите бота
```bash
python main.py
```

## 🐳 Docker

```bash
# Сборка и запуск
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

## 📖 Команды

### Для пользователей:
- `/start` — приветствие (текст настраивается через админ-панель)
- `/help` — справка (текст настраивается через админ-панель)
- `/close` — закрыть свой тикет
- `/reopen` — переоткрыть последний закрытый тикет

### Для администраторов:
- `/admin` — открыть админ-панель
- `/open_tickets` — список всех открытых тикетов

## ⚙️ Админ-панель

Доступна через команду `/admin`:

- **✏️ Изменить приветствие** — изменить текст команды /start
- **✏️ Изменить справку** — изменить текст команды /help
- **📁 Режим топиков** — переключить между "Отдельный топик" и "Общий топик"
- **📊 Открытые тикеты** — просмотр всех открытых тикетов

## 🎯 Как это работает

1. **Пользователь пишет боту** → сообщение пересылается в чат поддержки с уникальным ID тикета и кнопками управления
2. **Оператор отвечает через Reply** → бот автоматически пересылает ответ пользователю
3. **Управление тикетом** → кнопки "Закрыть/Открыть" и "Забанить/Разбанить" прямо в сообщении
4. **Режим отдельного топика** → для каждого пользователя создаётся отдельный топик в форуме, название обновляется при изменении статуса

## 📁 Структура проекта

```
supportticketbot/
├── main.py                 # Точка входа
├── config.py               # Конфигурация
├── requirements.txt        # Зависимости
│
├── database/
│   ├── models.py           # Модели SQLAlchemy
│   └── connection.py       # Подключение к БД
│
├── handlers/
│   ├── user_handlers.py    # Обработка сообщений от пользователей
│   ├── support_handlers.py # Обработка Reply в чате поддержки
│   └── admin_handlers.py   # Админ-панель
│
└── services/
    └── ticket_service.py   # Бизнес-логика
```

## 🔧 Настройка чата поддержки

Для использования режима "Отдельный топик для каждого":

1. Создайте супергруппу
2. Включите топики (Topics) в настройках группы
3. Добавьте бота в группу как администратора
4. Укажите `SUPPORT_CHAT_ID` в `.env`

## 📄 Лицензия

MIT
