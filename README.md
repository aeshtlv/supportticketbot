# Telegram Support Bot для shftsecure

Telegram-бот для системы поддержки с использованием топиков форума.

## 🎯 Архитектура

- **Пользователи** пишут боту в личные сообщения
- **Администраторы** работают через Telegram-группу с включёнными Topics (Forum)
- Каждый тикет = один топик в админ-группе

## 🚀 Возможности

- Автоматическое создание топика для каждого пользователя
- Пересылка сообщений пользователей в топики
- Автоматическая пересылка ответов администраторов пользователям
- Закрытие тикетов командой `/close`
- Поддержка всех типов медиа (текст, фото, видео, документы, голосовые и т.д.)

## 📋 Установка

### 1. Установите зависимости
```bash
pip install -r requirements.txt
```

### 2. Создайте файл `.env`
```env
BOT_TOKEN=your_telegram_bot_token_here
ADMIN_GROUP_ID=-1001234567890
ADMIN_IDS=123456789,987654321
```

- `BOT_TOKEN` — токен бота от [@BotFather](https://t.me/BotFather)
- `ADMIN_GROUP_ID` — ID группы с включёнными Topics (Forum)
- `ADMIN_IDS` — Telegram ID администраторов через запятую

### 3. Запустите бота
```bash
python main.py
```

## 🐳 Docker

```bash
docker-compose up -d --build
docker-compose logs -f
```

## 📖 Использование

### Для пользователей:
- Напишите боту в личные сообщения
- Бот автоматически создаст тикет и топик
- Администратор ответит вам

### Для администраторов:
- Работайте в админ-группе с топиками
- Отвечайте прямо в топике — сообщение автоматически пересётся пользователю
- Используйте `/close` в топике для закрытия тикета

## 📁 Структура проекта

```
supportticketbot/
├── main.py                 # Точка входа
├── config.py               # Конфигурация
├── requirements.txt        # Зависимости
│
├── database/
│   ├── models.py           # Модели (Ticket)
│   └── connection.py       # Подключение к БД
│
├── handlers/
│   ├── user_handlers.py    # Обработка сообщений пользователей
│   └── admin_handlers.py   # Обработка сообщений администраторов
│
└── services/
    └── ticket_service.py   # Бизнес-логика тикетов
```

## 🔧 Настройка админ-группы

1. Создайте супергруппу в Telegram
2. Включите Topics (Forum) в настройках группы
3. Добавьте бота в группу как администратора
4. Укажите `ADMIN_GROUP_ID` в `.env`

## 📄 Лицензия

MIT
